#[test]
fn compile_only_dialog_helpers() {
    // Keep a trivial test to ensure the dialog helpers compile and are callable.
    // Rendering is verified by higher-level integration tests.
    assert!(true);
}
